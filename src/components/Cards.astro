---
const posts = (await Astro.glob('../pages/posts/*.md'))
---
<ul class="grid grid-cols-1 md:grid-cols-2 gap-4">
    {
        posts.map(post => (
            <a href={post.url}>
                <div class="ring-1 rounded-sm post">
                    <h3>{post.frontmatter.title}</h3>
                    {post.frontmatter.description && (
                          <h5 class="opacity-70">
                        {post.frontmatter.description}
                          </h5>)}
                </div>
            </a>
        ))
    }
</ul>